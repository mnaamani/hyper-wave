// Suite runner for hyperwave-app-core: import every suite so brittle aggregates them into one TAP
// stream (`bare test.js`, or `npm test` from the repo root). Add new suites here.
import './lib/app-core.test.js';
import './lib/wallet-meta.test.js';
import './lib/topic.test.js';
