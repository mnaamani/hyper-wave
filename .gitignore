# dependencies + build output (matched at any depth: root + every workspace)
node_modules/
out/
bundles/

# local-only design/notes + spikes
spike/
DEMO.md
VULN.md
presentation/
demo/
/scratch

# NSFW classifier bundle — generated by scripts/build-nsfw.mjs at postinstall (esbuild: tfjs +
# nsfwjs + mobilenet_v2 model). Not committed (4.7 MB, reproducible); rebuilt on npm install.
apps/desktop/renderer/vendor/nsfw.bundle.js
apps/desktop/renderer/vendor/qr.bundle.js
